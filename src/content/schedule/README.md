# Schedule entries

Create one JSON file per date and segment through Decap CMS. No active schedule is supplied because the church calendar has not been approved.

Filename pattern: `YYYY-MM-DD-segment.json`.
